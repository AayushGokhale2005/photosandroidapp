<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tag type (key)"
        android:textStyle="bold"
        android:textSize="13sp"
        android:layout_marginBottom="4dp" />

    <!-- Editable autocomplete — type an existing type or a brand-new one -->
    <AutoCompleteTextView
        android:id="@+id/autoCompleteTagType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="e.g. person, location, event, year…"
        android:inputType="text"
        android:completionThreshold="1"
        android:layout_marginBottom="12dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Value"
        android:textStyle="bold"
        android:textSize="13sp"
        android:layout_marginBottom="4dp" />

    <EditText
        android:id="@+id/editTagValue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:hint="e.g. Alice, New York, Summer 2025" />

</LinearLayout>
