<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base light theme — primary color overridden at runtime by SettingsManager -->
    <style name="Theme.Photos" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">#FF6200EE</item>
        <item name="colorPrimaryVariant">#FF3700B3</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">#FF03DAC5</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#FF3700B3</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
    </style>

    <!-- Transparent dialog (used for color picker) -->
    <style name="Dialog_Transparent" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
